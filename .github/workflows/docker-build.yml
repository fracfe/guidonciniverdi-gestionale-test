name: Build Docker Image

on:
  push:
    branches: [ "main" ]

jobs:
  verify:
    uses: ./.github/workflows/ci.yml
    permissions:
      contents: read

  build-gestionale:
    needs: verify
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
    - uses: actions/checkout@v4

    - name: Login to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build Gestionale Image
      run: |
        docker build . \
          --file gestionale/Dockerfile \
          --tag ghcr.io/egit-guidoncini-verdi/guidonciniverdi-gestionale:${{ github.sha }} \
          --tag ghcr.io/egit-guidoncini-verdi/guidonciniverdi-gestionale:latest

    - name: Push Gestionale Image
      run: |
        docker push ghcr.io/egit-guidoncini-verdi/guidonciniverdi-gestionale:${{ github.sha }}
        docker push ghcr.io/egit-guidoncini-verdi/guidonciniverdi-gestionale:latest

  build-daemon:
    needs: verify
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
    - uses: actions/checkout@v4

    - name: Login to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
      
    - name: Build Gestionale Daemon Image
      run: |
        docker build . \
          --file gestionale-daemon/Dockerfile \
          --tag ghcr.io/egit-guidoncini-verdi/guidonciniverdi-gestionale-daemon:${{ github.sha }} \
          --tag ghcr.io/egit-guidoncini-verdi/guidonciniverdi-gestionale-daemon:latest

    - name: Push Gestionale Daemon Image
      run: |
        docker push ghcr.io/egit-guidoncini-verdi/guidonciniverdi-gestionale-daemon:${{ github.sha }}
        docker push ghcr.io/egit-guidoncini-verdi/guidonciniverdi-gestionale-daemon:latest
